# The asset directory is the repo root (netlify.toml used publish = "."), so
# everything that is tooling rather than website must be excluded from upload.
.git
.github
.claude
.gitignore
.assetsignore
netlify.toml
wrangler.jsonc
# Decap CMS admin — depends on Netlify git-gateway, parked until/unless it is
# migrated to a GitHub-backend OAuth flow. Kept in the repo, not deployed.
admin
dev_server.py
node_modules
.DS_Store
Thumbs.db
